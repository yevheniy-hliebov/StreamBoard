<UserControl x:Class="StreamBoard.Features.Home.Components.HomeBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StreamBoard.Features.Home.Components"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">

    <Border Height="112" CornerRadius="8" SnapsToDevicePixels="True">
        <Border.Background>
            <ImageBrush ImageSource="pack://application:,,,/StreamBoard;component/Assets/Images/banner_background.png"
                        Stretch="UniformToFill"/>
        </Border.Background>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0"
                   Source="pack://application:,,,/StreamBoard;component/Assets/Images/logo.png"
                   Height="80" 
                   Width="80"
                   Margin="16,0,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality" />

            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="12,0,0,0">

                <TextBlock Text="StreamBoard" 
                           Foreground="White" 
                           FontSize="40" 
                           FontWeight="SemiBold" 
                           LineHeight="52" 
                           Margin="0,0,0,12" />

                <TextBlock Text="0.1.0" 
                           Foreground="White"
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           LineHeight="20" 
                           Margin="0,0,0,12" />
            </StackPanel>

            <TextBlock Grid.Column="1"
                       Text="Created by yevheniy-hliebov"
                       Foreground="White" 
                       FontSize="12"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Opacity="0.7"
                       Margin="0,0,16,16">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black"
                          Opacity="0.63"
                          BlurRadius="4"
                          ShadowDepth="2.8"
                          Direction="315" />
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
