<UserControl x:Class="StreamBoard.Features.Decks.Views.Components.DeckPageItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             xmlns:controls="clr-namespace:StreamBoard.Components.Controls"
             x:Name="Root">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TitleLabel" 
                   Text="{Binding Title, ElementName=Root}" 
                   VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True" />
                                <Condition Binding="{Binding DataContext.IsRenameMode, RelativeSource={RelativeSource AncestorType=ListView}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Collapsed" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <ui:TextBox x:Name="EditBox"
                    Text="{Binding Title, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"
                    VerticalAlignment="Center"
                    Padding="2,0"
                    MinHeight="0">
            <ui:TextBox.Style>
                <Style TargetType="ui:TextBox" BasedOn="{StaticResource {x:Type ui:TextBox}}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True" />
                                <Condition Binding="{Binding DataContext.IsRenameMode, RelativeSource={RelativeSource AncestorType=ListView}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </ui:TextBox.Style>
        </ui:TextBox>

        <controls:FluentIcon Grid.Column="1" 
                             IconType="GripperBarHorizontal" 
                             FontSize="14" 
                             Cursor="Hand"
                             Margin="5,0,0,0"
                             dd:DragDrop.IsDragSource="True"/>
    </Grid>
</UserControl>