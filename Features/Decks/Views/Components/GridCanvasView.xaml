<UserControl x:Class="StreamBoard.Features.Decks.Views.Components.GridCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Canvas x:Name="Viewport"
                Grid.Row="0" Grid.RowSpan="2"
                Background="Transparent"
                ClipToBounds="True"
                VerticalAlignment="Stretch"
                HorizontalAlignment="Stretch">
            
            <Border x:Name="DeckSurface"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    RenderTransformOrigin="0,0">

                <Border.RenderTransform>
                    <TranslateTransform x:Name="PanTransform"/>
                </Border.RenderTransform>


                <ItemsControl ItemsSource="{Binding Cells}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="{Binding SelectedGrid.Rows}"
                                 Columns="{Binding SelectedGrid.Columns}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="45"
                                    Height="45"
                                    Margin="4"
                                    Background="#2B2B2B"
                                    BorderBrush="#555"
                                    BorderThickness="1"
                                    CornerRadius="4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </Border>
        </Canvas>

        <ComboBox Grid.Row="0" 
                  ItemsSource="{Binding Grids}"
                  SelectedItem="{Binding SelectedGrid, Mode=TwoWay}"
                  DisplayMemberPath="Name"
                  Width="110"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Left"
                  Margin="12"/>

    </Grid>
</UserControl>