<ContentControl x:Class="StreamBoard.Components.Controls.ReorderList.ReorderListItemWrapper"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:controls="clr-namespace:StreamBoard.Components.Controls"
                xmlns:local="clr-namespace:StreamBoard.Components.Controls.ReorderList">

    <ContentControl.Template>
        <ControlTemplate TargetType="{x:Type local:ReorderListItemWrapper}">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" VerticalAlignment="Center" />

                <controls:FluentIcon Grid.Column="1"
                                     IconType="GripperBarHorizontal"
                                     FontSize="14"
                                     Cursor="Hand"
                                     Margin="5,0,0,0">
                    <controls:FluentIcon.Style>
                        <Style TargetType="{x:Type controls:FluentIcon}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowDragHandle, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:ReorderListItemWrapper}}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </controls:FluentIcon.Style>
                </controls:FluentIcon>
            </Grid>
        </ControlTemplate>
    </ContentControl.Template>
</ContentControl>