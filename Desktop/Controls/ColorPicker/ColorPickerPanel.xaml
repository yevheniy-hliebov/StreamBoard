<UserControl x:Class="StreamTabula.Controls.ColorPicker.ColorPickerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             x:Name="RootColorPickerPanel">

    <UserControl.Resources>
        <Style x:Key="PaletteButtonStyle"
               TargetType="Button">
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="2"
                                BorderBrush="Transparent" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource TextFillColorPrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThumbOnlySliderStyle"
               TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>

                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>

                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16"
                                                         Height="16"
                                                         Fill="White"
                                                         Stroke="#E5E5E5"
                                                         StrokeThickness="1"
                                                         Cursor="Hand">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect BlurRadius="4"
                                                                          ShadowDepth="1"
                                                                          Opacity="0.4"
                                                                          Direction="270" />
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Border Height="40"
                CornerRadius="6"
                Background="{Binding SelectedBrush, ElementName=RootColorPickerPanel}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                Margin="0,0,0,16" />

        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="HEX"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontSize="12" />
            <ui:TextBox Grid.Column="1"
                        Text="{Binding SelectedColor, ElementName=RootColorPickerPanel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        ClearButtonEnabled="True" />
        </Grid>

        <!-- HSV -->
        <Grid Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="45" />
            </Grid.ColumnDefinitions>

            <!-- H (Hue) -->
            <TextBlock Text="H"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontWeight="Bold" />
            <Grid Grid.Column="1"
                  Margin="8,4,8,4">
                <Border CornerRadius="4"
                        Height="12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0"
                                             EndPoint="1,0">
                            <GradientStop Color="#FFFF0000"
                                          Offset="0" />
                            <GradientStop Color="#FFFFFF00"
                                          Offset="0.167" />
                            <GradientStop Color="#FF00FF00"
                                          Offset="0.333" />
                            <GradientStop Color="#FF00FFFF"
                                          Offset="0.5" />
                            <GradientStop Color="#FF0000FF"
                                          Offset="0.667" />
                            <GradientStop Color="#FFFF00FF"
                                          Offset="0.833" />
                            <GradientStop Color="#FFFF0000"
                                          Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Slider Minimum="0"
                        Maximum="360"
                        Value="{Binding Hue, ElementName=RootColorPickerPanel}"
                        Style="{StaticResource ThumbOnlySliderStyle}"
                        VerticalAlignment="Center"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1" />
            </Grid>
            <TextBlock Grid.Column="2"
                       Text="{Binding Hue, ElementName=RootColorPickerPanel, StringFormat={}{0:F0}°}"
                       VerticalAlignment="Center"
                       TextAlignment="Right" />

            <!-- S (Saturation) -->
            <TextBlock Grid.Row="1"
                       Text="S"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontWeight="Bold" />
            <Grid Grid.Row="1"
                  Grid.Column="1"
                  Margin="8,4,8,4">
                <Border CornerRadius="4"
                        Height="12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0"
                                             EndPoint="1,0">
                            <GradientStop Color="#FFFFFFFF"
                                          Offset="0" />
                            <GradientStop Color="{Binding PureHueColor, ElementName=RootColorPickerPanel}"
                                          Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Slider Minimum="0"
                        Maximum="100"
                        Value="{Binding Saturation, ElementName=RootColorPickerPanel}"
                        Style="{StaticResource ThumbOnlySliderStyle}"
                        VerticalAlignment="Center"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1" />
            </Grid>
            <TextBlock Grid.Row="1"
                       Grid.Column="2"
                       Text="{Binding Saturation, ElementName=RootColorPickerPanel, StringFormat={}{0:F0}%}"
                       VerticalAlignment="Center"
                       TextAlignment="Right" />

            <!-- V (Value/Brightness) -->
            <TextBlock Grid.Row="2"
                       Text="V"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       FontWeight="Bold" />
            <Grid Grid.Row="2"
                  Grid.Column="1"
                  Margin="8,4,8,4">
                <Border CornerRadius="4"
                        Height="12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0"
                                             EndPoint="1,0">
                            <GradientStop Color="#FF000000"
                                          Offset="0" />
                            <GradientStop Color="{Binding PureHueColor, ElementName=RootColorPickerPanel}"
                                          Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Slider Minimum="0"
                        Maximum="100"
                        Value="{Binding Brightness, ElementName=RootColorPickerPanel}"
                        Style="{StaticResource ThumbOnlySliderStyle}"
                        VerticalAlignment="Center"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1" />
            </Grid>
            <TextBlock Grid.Row="2"
                       Grid.Column="2"
                       Text="{Binding Brightness, ElementName=RootColorPickerPanel, StringFormat={}{0:F0}%}"
                       VerticalAlignment="Center"
                       TextAlignment="Right" />
        </Grid>

        <!-- Presset Colors -->
        <ItemsControl ItemsSource="{Binding PresetColors, ElementName=RootColorPickerPanel}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="5" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource PaletteButtonStyle}"
                            Background="{Binding Brush}"
                            Width="36"
                            Height="36"
                            Margin="4"
                            BorderThickness="0"
                            Click="OnPaletteColorClick"
                            Tag="{Binding Hex}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>