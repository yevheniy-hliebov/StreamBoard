<UserControl x:Class="StreamTabula.Features.Servers.Components.HttpRequestLogList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:StreamTabula.Features.Servers.Components"
             x:Name="Root">

    <Grid Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasItems, ElementName=LogItems}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="TimeCol"/>
                <ColumnDefinition Width="Auto" SharedSizeGroup="StatusCol"/>
                <ColumnDefinition Width="60"   SharedSizeGroup="MethodCol"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="IpCol"/>
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </Grid.Resources>

            <TextBlock Grid.Column="0" Text="TIME" Margin="0,0,12,0"/>
            <TextBlock Grid.Column="1" Text="STATUS" Margin="0,0,12,0"/>
            <TextBlock Grid.Column="2" Text="METHOD"/>
            <TextBlock Grid.Column="3" Text="ENDPOINT" Margin="0,0,12,0"/>
            <TextBlock Grid.Column="4" Text="CLIENT IP" HorizontalAlignment="Right"/>
        </Grid>

        <ItemsControl Grid.Row="1" 
                      x:Name="LogItems"
                      ItemsSource="{Binding Logs, ElementName=Root}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:HttpRequestLogItem />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="1" 
                   Text="No requests yet" 
                   HorizontalAlignment="Center" 
                   Foreground="Gray"
                   Margin="0,20,0,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasItems, ElementName=LogItems}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

    </Grid>
</UserControl>
