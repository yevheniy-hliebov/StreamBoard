namespace StreamTabula.Features.Integrations.Common.Models
{
    public enum ConnectionState
    {
        NotConnected,
        Connecting,
        Connected,
        Disconnecting,
        Failed
    }
}
