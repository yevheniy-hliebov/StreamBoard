<UserControl x:Class="StreamTabula.Components.Tray.TrayIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar">

    <Grid>
        <tb:TaskbarIcon x:Name="NotifyIcon" 
                        ToolTipText="StreamTabula" 
                        IconSource="pack://application:,,,/StreamTabula;component/Assets/Images/logo.ico"
                        TrayLeftMouseDown="NotifyIcon_TrayLeftMouseDown">

            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <ui:MenuItem Header="Open" Click="OpenMenuItem_Click"/>
                    <Separator />
                    <ui:MenuItem Header="Exit" Click="ExitMenuItem_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>

        </tb:TaskbarIcon>
    </Grid>
</UserControl>