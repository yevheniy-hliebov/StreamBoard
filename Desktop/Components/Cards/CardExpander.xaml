<UserControl x:Class="StreamTabula.Components.Cards.CardExpander"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:StreamTabula.Components.Cards"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Name="Root">

    <ui:CardExpander IsExpanded="{Binding IsExpanded, ElementName=Root}">
        <ui:CardExpander.Header>
            <StackPanel>
                <TextBlock Text="{Binding Title, ElementName=Root}" 
                FontSize="18" 
                FontWeight="SemiBold" />

                <TextBlock Text="{Binding Description, ElementName=Root}" 
                FontSize="14" 
                FontWeight="Regular"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                TextWrapping="Wrap"/>
            </StackPanel>
        </ui:CardExpander.Header>

        <ContentPresenter Content="{Binding InnerContent, ElementName=Root}" />
    </ui:CardExpander>
</UserControl>
